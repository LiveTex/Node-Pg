{
  "name": "node-pg",
  "main": "bin/index.js",
  "version": "0.1.0",

  "scripts": {
    "prepublish": "make clean && make check && make",
    "preinstall": "sudo apt-get install libpq-dev libjemalloc-dev libv8-dev",
    "install": "make native"
  },

  "dependencies": {
    "node-util": "*"
  }
}
