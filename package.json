{
  "name": "livetex-node-pg",
  "main": "bin/index.js",
  "version": "1.0.0",

  "scripts": {
    "prepublish": "make clean && make js-check && make js-lint && make js-build",
    "preinstall": "sudo apt-get -y install libpq-dev libjemalloc-dev libv8-dev",
    "install": "make native-build"
  }
}
